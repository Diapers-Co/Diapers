import 'package:flutter/material.dart';

class AllowGPS extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("We are getting you GPS location... please wait"),      ),
    );
  }
}