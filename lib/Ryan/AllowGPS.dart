import 'package:flutter/material.dart';


class AllowGPS extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Please make sure that you have enabled GPS")),
    );
  }
}